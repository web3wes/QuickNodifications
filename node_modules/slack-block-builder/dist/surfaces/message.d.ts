import { SurfaceBuilderBase } from 'slack-block-builder/dist/internal/base';
import { SlackMessageDto } from 'slack-block-builder/dist/internal/dto';
import { AsUser, Attachments, Blocks, Channel, DeleteOriginal, Ephemeral, IgnoreMarkdown, InChannel, PostAt, ReplaceOriginal, Text, ThreadTs, Ts, BuildToJSON, BuildToObject, GetAttachments, GetBlocks, GetPreviewUrl, PrintPreviewUrl } from 'slack-block-builder/dist/internal/methods';
import type { BlockBuilder } from 'slack-block-builder/dist/internal/types';
export interface MessageParams {
    channel?: string;
    postAt?: number;
    text?: string;
    threadTs?: string;
    ts?: string;
}
export interface MessageBuilder extends AsUser, Attachments, Blocks<BlockBuilder>, Channel, DeleteOriginal, Ephemeral, IgnoreMarkdown, InChannel, PostAt, ReplaceOriginal, Text, ThreadTs, Ts, BuildToJSON, BuildToObject<SlackMessageDto>, GetAttachments, GetBlocks, GetPreviewUrl, PrintPreviewUrl {
}
/**
 * @@link https://api.slack.com/messaging/composing
 * @@displayName Message
 */
export declare class MessageBuilder extends SurfaceBuilderBase {
}
