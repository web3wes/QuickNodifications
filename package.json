{
  "name": "vip_notifier",
  "version": "1.0.0",
  "description": "Slack app to notify vip users",
  "main": "VIPNotifier.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "DH",
  "license": "ISC",
  "dependencies": {
    "@slack/bolt": "^3.12.1",
    "dotenv": "^16.0.1"
  }
}
