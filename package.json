{
  "name": "ondeck",
  "version": "1.0.0",
  "description": "On deck slack bot. Used to notify active users.",
  "main": "onDeck.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "DH",
  "license": "ISC",
  "dependencies": {
    "@slack/bolt": "^3.12.1",
    "dotenv": "^16.0.1"
  }
}
